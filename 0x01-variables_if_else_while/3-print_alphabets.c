#include <stdio.h>
/**
 * main - print if number is positive, negative or zero
 *
 * Description: Using the main function
 * Return: 0
 */
int main(void)

{
	char ch;

	for (ch - 'a' ; ch < -'z', c++)

		putchar(ch);
	(ch - 'A' ; ch < -'Z', c++)

		putchar(ch);

	        putchar ('n\');
		return(0);

}
